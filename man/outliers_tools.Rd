% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R, R/retrieve.R
\name{export_outliers}
\alias{export_outliers}
\alias{import_outliers}
\alias{outliers_tools}
\alias{retrieve_outliers}
\title{Manage outliers from JDemetra+ workspaces}
\usage{
export_outliers(x, path = NULL, verbose = TRUE)

import_outliers(path, verbose = TRUE)

retrieve_outliers(
  jws,
  domain = TRUE,
  estimation = FALSE,
  point = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{x}{\link{list} A list of outliers, as returned by
\code{\link[=retrieve_outliers]{retrieve_outliers()}}.}

\item{path}{[\link[base]{character}] Path to a YAML file to write to
or read from. If missing, defaults to
\code{"regression/outliers_<random_name>.yaml"}.}

\item{verbose}{[\link[base]{logical}] Whether to print informative
messages (default: \code{TRUE}).}

\item{jws}{A Java Workspace object, as returned by \code{\link[rjd3workspace:jws_open]{rjd3workspace::jws_open()}} or
\code{\link[rjd3workspace:jws_new]{rjd3workspace::jws_new()}}.}

\item{domain}{Boolean indicating if the outliers should be extracted from the
domain specification.}

\item{estimation}{Boolean indicating if the outliers should be extracted from the
estimation specification.}

\item{point}{Boolean indicating if the outliers should be extracted from the
point specification.}
}
\value{
\itemize{
\item \code{retrieve_outliers()} returns a named list where each element
corresponds to a series in the workspace and contains the names
of existing (detected or pre-specified) outliers.
\item \code{export_outliers()} invisibly returns the path of the YAML file
written.
\item \code{import_outliers()} returns a list of outliers read from a YAML file.
}
}
\description{
These functions allow extracting, exporting and importing outliers
detected in a JDemetra+ workspace:
\itemize{
\item \code{\link[=retrieve_outliers]{retrieve_outliers()}} extracts outliers from a \code{.xml} workspace.
\item \code{\link[=export_outliers]{export_outliers()}} saves extracted outliers into a YAML file.
\item \code{\link[=import_outliers]{import_outliers()}} loads outliers back to a \code{.xml} workspace from a YAML file.
}

They are useful for archiving and reusing outliers detected
during seasonal adjustment workflows.
}
\examples{
library("rjd3workspace")
# Load a Workspace
file <- system.file("workspaces", "workspace_test.xml", package = "rjd3workspace")
jws <- jws_open(file)
outliers_table <- retrieve_outliers(jws, domain = FALSE, point = TRUE)

path_outliers <- tempfile(pattern = "outliers-table", fileext = ".yaml")
export_outliers(outliers_table, path_outliers)
outliers_table_imported <- import_outliers(path = path_outliers)

}
