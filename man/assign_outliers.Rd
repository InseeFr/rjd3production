% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign.R
\name{assign_outliers}
\alias{assign_outliers}
\title{Assign outliers to a JDemetra+ workspace}
\usage{
assign_outliers(jws, outliers)
}
\arguments{
\item{jws}{A Java Workspace object, as returned by \code{\link[rjd3workspace:jws_open]{rjd3workspace::jws_open()}} or
\code{\link[rjd3workspace:jws_new]{rjd3workspace::jws_new()}}.}

\item{outliers}{[\link[base]{list}] A named list where each element
corresponds to a series in the workspace (created by \link{retrieve_outliers} or
\link{import_outliers}).}
}
\value{
The object \code{jws} updated with new prespecified outliers.
}
\description{
This function updates a JDemetra+ workspace (\code{.xml}) by inserting
pre-specified outliers into the \code{domainSpec} of each seasonal adjustment
model (SAI).
}
\details{
This function only modify the first SA-Processing.
}
\examples{
\dontrun{
# Retrieve outliers from an existing workspace
outs <- retrieve_outliers("workspace.xml")

# Reapply them to another workspace
assign_outliers(outliers = outs, ws_path = "workspace.xml")
}

}
\seealso{
\itemize{
\item \code{\link[=retrieve_outliers]{retrieve_outliers()}} to extract outliers from an existing workspace.
\item \code{\link[=export_outliers]{export_outliers()}} / \code{\link[=import_outliers]{import_outliers()}} to manage YAML files of outliers.
}
}
