% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R, R/retrieve.R
\name{export_td}
\alias{export_td}
\alias{import_td}
\alias{td_tools}
\alias{retrieve_td}
\title{Manage trading-day regressors (TD) from JDemetra+ workspaces}
\usage{
export_td(x, path = NULL, verbose = TRUE)

import_td(path, verbose = TRUE)

retrieve_td(
  jws,
  domain = TRUE,
  estimation = FALSE,
  point = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{x}{[\link[base]{list} | \link[base]{data.frame}] An object containing
the TD information, typically the output of \code{\link[=retrieve_td]{retrieve_td()}}.}

\item{path}{[\link[base]{character}] Path to a YAML file to write to
or read from. If missing, defaults to
\code{"regression/td_<ws_name>.yaml"}.}

\item{verbose}{[\link[base]{logical}] Whether to print informative
messages (default: \code{TRUE}).}

\item{jws}{A Java Workspace object, as returned by \code{\link[rjd3workspace:jws_open]{rjd3workspace::jws_open()}} or
\code{\link[rjd3workspace:jws_new]{rjd3workspace::jws_new()}}.}

\item{domain}{Boolean indicating if the outliers should be extracted from the
domain specification.}

\item{estimation}{Boolean indicating if the outliers should be extracted from the
estimation specification.}

\item{point}{Boolean indicating if the outliers should be extracted from the
point specification.}
}
\value{
\itemize{
\item \code{retrieve_td()} returns a \link{data.frame} with columns:
\itemize{
\item \code{series}: series names,
\item \code{regs}: TD regressor sets,
}
\item \code{export_td()} invisibly returns the path of the YAML file written.
\item \code{import_td()} returns a list or data structure read from YAML.
}
}
\description{
These functions allow to extract, export and import the
calendar (TD) regressors used in JDemetra+ workspaces:
\itemize{
\item \code{\link[=retrieve_td]{retrieve_td()}} extracts the TD specification from a \code{.xml} workspace.
\item \code{\link[=export_td]{export_td()}} saves extracted TD information into a YAML file.
\item \code{\link[=import_td]{import_td()}} loads TD information back from a YAML file.
}

They are useful for documenting and reusing the regression settings
applied in seasonal adjustment workflows.
}
\examples{
library("rjd3workspace")
# Load a Workspace
file <- system.file("workspaces", "workspace_test.xml", package = "rjd3workspace")
jws <- jws_open(file)
td_table <- retrieve_td(jws, domain = FALSE, point = TRUE)

path_td <- tempfile(pattern = "td-table", fileext = ".yaml")
export_td(td_table, path_td)
td_table_imported <- import_td(path = path_td)

}
