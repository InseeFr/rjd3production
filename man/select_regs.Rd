% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{select_regs}
\alias{select_regs}
\title{Select Calendar Regressors for One or Multiple Series}
\usage{
select_regs(series, context = NULL, ...)
}
\arguments{
\item{series}{[\link[stats]{ts} or mts or matrix or \link[base]{data.frame}] A univariate time series (\code{ts}) or a
multivariate series (columns as separate series).}

\item{context}{\link{list} Modeling context created by
\code{\link[rjd3toolkit:modelling_context]{rjd3toolkit::modelling_context()}}.}

\item{...}{Additional arguments passed to \code{\link[=create_specs_set]{create_specs_set()}} controlling
the generation of X13 specifications. Possible arguments include:
\describe{
\item{outliers}{Optional list of outliers with elements \code{type} (vector of types, e.g., "AO", "LS", "TC") and \code{date} (vector of dates).}
\item{span_start}{Starting date of the estimation (character, format \code{"YYYY-MM-DD"}).}
\item{...}{Other arguments accepted by \code{\link[=create_specs_set]{create_specs_set()}}.}
}}
}
\value{
A data.frame with two columns:
\describe{
\item{series}{Name of the series (column name if \code{series} is multivariate).}
\item{reg_selected}{Name of the selected regressor set.}
}
}
\description{
Applies the X13 regression selection procedure to one or more time series.
If multiple series are provided as columns of a matrix or data.frame, each series
is processed separately. The function returns the selected set of regressors for each series.
}
\examples{
# Single series
select_regs(AirPassengers)

# Multiple series
select_regs(Seatbelts[, -8])

# Restrict regressors sets
my_context <- create_insee_context()
my_context$variables <- my_context$variables[c("REG1", "REG1_LY", "REG6", "REG6_LY")]
select_regs(Seatbelts[, -8], context = my_context)

}
